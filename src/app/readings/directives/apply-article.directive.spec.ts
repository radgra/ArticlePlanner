import { ApplyArticleDirective } from './apply-article.directive';

describe('ApplyArticleDirective', () => {
  it('should create an instance', () => {
    const directive = new ApplyArticleDirective();
    expect(directive).toBeTruthy();
  });
});
